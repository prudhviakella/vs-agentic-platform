# requirements-dev.txt — Install all packages in editable mode for local development
#
# Run once from the vs-agentic-platform/ root:
#   pip install -r requirements-dev.txt
#
# This installs:
#   1. vs-agent-core      — base package (editable)
#   2. clinical_trial_agent — pharma domain agent (editable)
#   3. platform           — FastAPI gateway (editable)
#   4. dev tooling        — pytest, uvicorn etc.

# ── Core packages (editable) ──────────────────────────────────────────────────
-e ./vs-agent-core
-e ./clinical_trial_agent
-e ./platform

# ── Dev tooling ───────────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
pytest-html>=4.0
httpx>=0.27
uvicorn[standard]>=0.30
